How to Use the Program.

Run main.py to start the program. Enter the number of processes with a minimum of 3. Fill in the following for each process:

Arrival Time,
Burst Time,
Priority (lower number = higher priority).

Enter the Time Quantum if using Round Robin or Priority with Round Robin. 

Select an option from the menu:

First-Come, First-Served (FCFS),
Shortest Job First (SJF),
Shortest Remaining Time (SRT),
Round Robin (RR),
Non-preemptive Priority Scheduling,
Priority Scheduling with Round Robin,
Re-enter process details,
Exit.

The user can also re-use the values that were entered previously and switch between cpu scheduling by pressing the number assigned to a specific algorithm without needing to exit the entire program to re-enter them for another algorithm.

View the results:

Gantt Chart
Table — Completion Time (CT), Arrival Time (AT), Burst Time (BT), Priority (PR), Waiting Time (WT), Turnaround Time (TAT) per process
— Average WT and Average TAT.
